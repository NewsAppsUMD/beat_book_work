[project]
name = "notus"
version = "0.1.0"
description = "Scrapers for NOTUS stories"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.12.3",
    "feedparser>=6.0.12",
    "requests>=2.32.5",
]

[project.scripts]
rss-parse = "rss_parser:main"
scrape = "scraper:main"
